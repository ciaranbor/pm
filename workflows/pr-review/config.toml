description = "Reviewer reviews a PR and reports findings to the user"
agents = ["reviewer"]
brief_agents = ["reviewer"]
when_to_use = "Reviewing an existing PR or branch, not producing new feature work."
