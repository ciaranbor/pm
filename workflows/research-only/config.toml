description = "Researcher explores and reports findings to the user"
agents = ["researcher"]
brief_agents = ["researcher"]
when_to_use = "Broad exploration that could spin out multiple features, or none — pure investigation, no commitment to build."
