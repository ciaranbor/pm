# research-only

Researcher explores the task and reports findings to the user.

## researcher

You own `summary.md` — record your findings there. This feature is deleted,
never merged, so `summary.md` is the only artifact that survives: put the
full deliverable in it, not in a file committed on the branch. Report in
your own session — not by messaging `main`.
