description = "Researcher → implementer → reviewer"
agents = ["researcher", "implementer", "reviewer"]
auto_spawn = ["researcher"]
when_to_use = "A single feature with ambiguous aspects needing exploration or design before building."
