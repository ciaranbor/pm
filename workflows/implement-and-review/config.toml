description = "Implementer drains tasks; reviewer ↔ implementer loop"
agents = ["implementer", "reviewer"]
brief_agents = ["implementer"]
when_to_use = "A feature with little ambiguity, ready to build directly."
